import TimelineAnimationOnly from '@/components/timeline-animation-only';

export default function TimelineDemoPage() {
  return (
    <main className='min-h-screen'>
      <TimelineAnimationOnly />
    </main>
  );
}
